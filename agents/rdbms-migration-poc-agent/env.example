# Copy to .env in this directory (gitignored). Used by `agentengine dev up` and headless CLI.
# See ../../docs/architecture.md for multi-agent state vs migration target.

# Merge demo Postgres into the Agentic dev stack (port 5433). First `agentengine dev up` may take
# ~1 min while init scripts load demo/seed.sql into a new volume.
COMPOSE_FILE=.agentengine/docker-compose.dev.yml:docker-compose.postgres.yml

# --- Migration target (deterministic runner: load PoC data) ---
# agentengine injects local Mongo for the platform checkpointer and may ignore process
# MONGODB_URI overrides. The runner prefers Atlas from this .env file when MONGODB_URI
# is mongodb+srv / *.mongodb.net (Playground migrate → Atlas). Optional explicit override:
#   MIGRATION_TARGET_MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/
# Atlas field demo:
#   MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/
#   MIGRATION_TARGET_DB=commercedb
MONGODB_URI=
MIGRATION_TARGET_DB=commerce_poc
# MIGRATION_TARGET_MONGODB_URI=

# --- Shared PoC pack state (MongoDB workspace when set) ---
# Artifacts (discovery, plan, HITL) go here — separate DB from migration target data.
# Required for multi-agent handoff on the same engagement/session.
MIGRATION_STATE_MONGODB_URI=
MIGRATION_STATE_DB=migration_poc_state
# If you only set MONGODB_URI, leave MIGRATION_STATE_MONGODB_URI empty unless you
# intentionally want the same cluster with a different database name above.

# --- Source PostgreSQL (demo after scripts/seed-postgres-demo.sh) ---
# agentengine dev (Docker): host.docker.internal — native CLI on host: localhost
POSTGRES_URI=postgresql://commerce:commerce@host.docker.internal:5433/commerce_demo

# --- Session file workspace (single-agent dev; not shared across hosts) ---
# MIGRATION_WORKSPACE_DIR=.agentengine/migration-sessions

OPENAI_API_KEY=
# OPENAI_BASE_URL=https://grove-gateway-prod.azure-api.net/grove-foundry-prod/openai/v1

GEMINI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
VOYAGE_API_KEY=
